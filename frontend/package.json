{
  "name": "qure-frontend",
  "version": "1.0.0",
  "description": "Qure Frontend Application",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint src --ext .js",
    "postinstall": "npm install @rollup/rollup-linux-x64-gnu --save-optional --force || true"
  },
  "dependencies": {
    "zustand": "^4.5.0"
  },
  "devDependencies": {
    "vite": "^5.1.0",
    "eslint": "^8.57.0"
  }
}

